import { TS_LINT_CONFIG_FILE_NAME } from "../../const";
import { Template } from "../Template";

export const tsLintTemplate = new Template(
  TS_LINT_CONFIG_FILE_NAME,
  TS_LINT_CONFIG_FILE_NAME,
  __dirname,
);
