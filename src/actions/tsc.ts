async function tsc() {
  /*   check_project();
  await ordering_npm_deps();
  await sync_npm_deps(false);
  await ts_tsLint_config_builder();
  await update_pkg();
  pre_publish_shell();
  tsc_compile(); */
}

tsc();
