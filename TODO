
Улучшения:
  ☐ Проверка на циркулярные зависимости (utils depend on isomorphis, isomorphic depend on utils);
  ☐ Добавил флаг при сборке бибилиотек, если он не стоит то бибилиотека подключается в index.html, если стоит то бибилиотека записывается в ts файл по которому можно загрузить бибилиотеку из кода.
  ☐ При сборке tsc необходимо синхронизировать директорию lib. dll_bundle и lib_bundle нет необходимоти синхронизировать;
  ☐ Доработать конфиг по умолчанию для tsc сборки;
  ☐ Проверка, установлены ли необходимые зависимости из списка sync_npm_deps;
  ☐ Проработать ситуацию когда в режиме разработки обновляется локальный dll. Нужно пересобрать dll и пересобрать index.html.

